<div class="dark:text-white">
    <h1 class="text-2xl font-bold mb-4">{{ __('What is an embedding?') }}</h1>
    <p class="text-lg">
        {{ __('Embeddings are vectors that represent real-world objects, such as words, images, or videos, in a form that machine learning models can process easily. Embeddings enable similarity searches and are fundamental to machine learning.') }}
    </p>
    <br />
    <p class="text-lg">
        {{ __('In the following example, we can see how using a embedding can change the style of an image.') }}
    </p>
    <h1 class="text-2xl font-bold mt-4 mb-4">{{ __('Example') }}:</h1>
    <img src="{{ asset('assets/info/embeddingexample.jpeg') }}" alt="lora" class="w-full mt-4" />
</div>
